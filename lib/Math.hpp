#include <chrono>

#pragma once

namespace math {
    
    long long gcd(long long n1, long long n2);

    long long lcm(long long n1, long long n2);

    long long setBitCount(long long n);

    long long setBitDiff(long long n1, long long n2);
    
}